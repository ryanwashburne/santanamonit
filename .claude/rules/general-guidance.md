# General Guidance

You are using Chakra v3, so always use the MCP tool to learn about the components before using them.

Whenever you are done, run `pnpm run check` and `pnpm run typecheck` to verify everything is linted, formatted, and typed correctly.